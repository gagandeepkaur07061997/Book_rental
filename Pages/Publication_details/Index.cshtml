@page
@model Book_rental.Pages.Publication_details.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Publication_detail[0].Books_Copies)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Publication_detail[0].Publisher_detail)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Publication_detail[0].Book_detailId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Publication_detail) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Books_Copies)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Publisher_detail.Publisher_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Book_detailId)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
